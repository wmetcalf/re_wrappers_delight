[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "Cython",
    "pybind11>=2.6.0",
    "ninja; sys_platform != 'Windows'",
    "cmake>=3.12",
]

build-backend = "setuptools.build_meta"
